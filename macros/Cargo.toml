[package]
name = "macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
mathfn = { path = "../mathfn" }
matrix = { path = "../matrix" }
utils = { path = "../utils" }
syn = { version = "2", features = ["full", "extra-traits"] }
quote = "1"
proc-macro2 = "1"